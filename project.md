# Профиль проекта

Всё, что отличает этот проект от шаблона. Заполняется при настройке ([START-HERE.md](START-HERE.md))
и дополняется по ходу работы. Runbook'и берут конкретику отсюда — сами они общие.
Секретов здесь не держать: токены и пароли — в `.env`, здесь только «где лежит».

```yaml
status: not-configured      # not-configured | configured
configured_at:              # дата завершения настройки
language: ru                # язык чек-листов
lens_model: opus            # opus | sonnet — модель суб-агентов-луп (sonnet экономит лимиты)
publish: none               # none — только файл | git — коммит и push checklists/ + wiki/
```

## Трекер

```yaml
tracker: jira-mcp           # jira-mcp — через MCP | paste — пользователь вставляет текст задачи
jira_url:                   # https://<company>.atlassian.net
project_key:                # MOB
mcp_read_tools:             # имена тулз чтения, проверенные на реальной задаче
  - 
```

## Репозитории приложения

Только чтение. Путь — локальная папка (абсолютный путь или `repos/<name>` внутри этого проекта).

| Имя | Путь | Что это | Стек |
|---|---|---|---|
| | | | |

```yaml
base_branch:                # от чего отводятся задачи: develop | dev | main
branch_pattern:             # как ключ попадает в имя ветки, напр. feature/<KEY>-*  или  <KEY>
commit_pattern:             # как ключ попадает в коммит, напр. "<KEY>: …" или "[<KEY>] …"
examples:                   # 2–3 реальные ветки задач, найденные при разведке
  - 
```

## Платформа и стек

```yaml
platform:                   # android | ios | web | backend | flutter | react-native
application_id:             # для мобилки: пакет приложения
min_sdk:                    # для Android
target_sdk:
build_variants:             # debug / qa / release, flavors
tests:                      # где юнит-тесты, где UI-тесты, чем запускаются
```

## Куда смотреть в коде

Таблица «что нужно узнать → где в ЭТОМ репо смотреть». Собирается при настройке из
`stack-hints/` и проверяется на реальных путях. Используется правилом R3 в
[runbooks/generate-checklist.md](runbooks/generate-checklist.md) и всеми лупами.

| Что нужно узнать | Куда смотреть |
|---|---|
| | |

## Стенд и устройства (фаза 2)

```yaml
devices:                    # реальный телефон / эмулятор / ферма; какие версии ОС обязательны
adb: no                     # yes — агент может сам работать с устройством через adb
install_build:              # как поставить сборку задачи (откуда APK, какой gradle-таск)
backend_stand:              # URL тестового бэкенда (без токенов)
backend_access:             # что есть: API, логи, БД на чтение; где лежат креды (.env)
roles:                      # роли пользователей в приложении
test_accounts:              # тестовые аккаунты — логин/роль, БЕЗ паролей
```

## Глоссарий

Термины, названия экранов/фич, принятые сокращения. Чек-лист пишется в этих терминах.

| Термин | Что значит / как писать в чек-листе |
|---|---|
| | |

## Адаптации

Чем эта копия отличается от шаблона: что поменяли в механизме и зачем. Одна строка — одно
изменение. Пишется при настройке и при `/qa-adapt`.

| Дата | Что изменили | Зачем |
|---|---|---|
| | | |
